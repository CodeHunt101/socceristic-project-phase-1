// if (streakType === 'largest_streak_wins' && type === 'max') {
//   document.querySelector('#more-facts').appendChild(document.createElement('ul')).textContent = `Largest win streak with ${biggestStreak} in total: `
//   streakTeams.forEach(team => {
//     document.querySelectorAll('#more-facts ul')[document.querySelectorAll('#more-facts ul').length-1].appendChild(document.createElement('li')).textContent = team
//   })
// }
// if (streakType === 'largest_streak_draws' && type === 'max') {
//   document.querySelector('#more-facts').appendChild(document.createElement('ul')).textContent = `Largest draw streak with ${biggestStreak} in total: `
//   streakTeams.forEach(team => {
//     document.querySelectorAll('#more-facts ul')[document.querySelectorAll('#more-facts ul').length-1].appendChild(document.createElement('li')).textContent = team
//   })
// }
// if (streakType === 'largest_streak_loses' && type === 'max') {
//   document.querySelector('#more-facts').appendChild(document.createElement('ul')).textContent = `Largest lose streak with ${biggestStreak} in total: `
//   streakTeams.forEach(team => {
//     document.querySelectorAll('#more-facts ul')[document.querySelectorAll('#more-facts ul').length-1].appendChild(document.createElement('li')).textContent = team
//   })
// }
// if (streakType === 'penalty' && type === 'max') {
//   document.querySelector('#more-facts').appendChild(document.createElement('ul')).textContent = `Most penalties conceded with ${biggestStreak} in total: `
//   streakTeams.forEach(team => {
//     document.querySelectorAll('#more-facts ul')[document.querySelectorAll('#more-facts ul').length-1].appendChild(document.createElement('li')).textContent = team
//   })
// }
// if (streakType === 'penalty' && type === 'min') {
//   document.querySelector('#more-facts').appendChild(document.createElement('ul')).textContent = `Fewest penalties conceded with ${biggestStreak} in total: `
//   streakTeams.forEach(team => {
//     document.querySelectorAll('#more-facts ul')[document.querySelectorAll('#more-facts ul').length-1].appendChild(document.createElement('li')).textContent = team
//   })
// }
// if (streakType === 'clean_sheet' && type === 'max') {
//   document.querySelector('#more-facts').appendChild(document.createElement('ul')).textContent = `Longest clean sheet streak with ${biggestStreak} in total: `
//   streakTeams.forEach(team => {
//     document.querySelectorAll('#more-facts ul')[document.querySelectorAll('#more-facts ul').length-1].appendChild(document.createElement('li')).textContent = team
//   })
// } 